<%#
## Local variables:

- `page`:
  An instance of [Administrate::Page::Show][1].
  Contains methods for accessing the resource to be displayed on the page,
  as well as helpers for describing how each attribute of the resource
  should be displayed.
  To access resource object, use `page.resource`.

%>

<% content_for(:title) { page.resource.title } %>

<header class="header">
  <h1 class="header-heading"><%= content_for(:title) %></h1>
  <%= link_to(
    "Edit",
    [:edit, Administrate::NAMESPACE, page.resource],
    class: "button",
  ) %>
</header>

<dl>
  <% page.attributes.each do |attribute| %>
    <dt class="attribute-label"><%= attribute.name.titleize %></dt>
    <dd class="attribute-data"><%= simple_format(render_field attribute) %></dd>
  <% end %>
</dl>
